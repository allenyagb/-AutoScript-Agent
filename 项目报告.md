# Linux系统管理与Shell编程课程项目报告

---

**课程名称**：Linux 系统管理与 Shell 编程

**项目名称**：AutoScript Agent —— 基于大语言模型的 Ubuntu 自主任务执行智能体

**班级**：[请填写]

**小组**：[请填写]

**成员信息**：

| 学号 | 姓名 |
|------|------|
| [请填写] | [请填写] |
| [请填写] | [请填写] |
| [请填写] | [请填写] |

**提交日期**：2026年6月

---

## 目录

1. [项目背景与需求分析](#1-项目背景与需求分析)
2. [项目目标](#2-项目目标)
3. [系统设计](#3-系统设计)
4. [功能实现](#4-功能实现)
5. [Linux技术使用情况](#5-linux技术使用情况)
6. [测试与验证](#6-测试与验证)
7. [开发过程与版本迭代](#7-开发过程与版本迭代)
8. [AI工具使用说明](#8-ai工具使用说明)
9. [项目总结](#9-项目总结)
10. [项目分工](#10-项目分工)
11. [参考文献](#11-参考文献)

---

## 1 项目背景与需求分析

### 1.1 服务对象

本项目面向以下用户群体：

- **Linux 服务器管理员**：需要快速执行系统巡检、日志分析、文件批处理等日常运维任务
- **树莓派/边缘设备开发者**：需要在资源受限设备上通过自然语言指令完成自动化操作
- **小型网站维护人员**：不具备深厚 Linux 命令行经验，希望通过自然语言完成服务器管理
- **运维新手**：希望借助 AI 辅助来学习和执行 Linux 系统管理命令

### 1.2 实际问题

本项目解决以下实际问题：

1. **命令记忆负担重**：Linux 系统管理涉及大量命令（grep、awk、sed、find 等）和参数组合，运维人员需要频繁查阅手册（man page），效率低下
2. **重复性任务耗时**：多台服务器需要重复执行相同的巡检、备份、日志分析命令，手动操作容易遗漏，缺乏自动化能力
3. **安全风险难以控制**：在终端直接执行命令时，误操作（如 `rm -rf /`）可能导致灾难性后果，缺少有效的安全审查机制
4. **脚本编写门槛高**：将运维需求转化为可执行脚本需要一定的编程能力，对于不熟悉 Shell/Python 编程的管理员来说门槛较高
5. **系统异常难以及时发现**：缺乏定时自动巡检机制，CPU 过载、磁盘空间不足等问题往往要等到用户反馈才能发现

### 1.3 现有方法分析

| 现有方法 | 不足 |
|----------|------|
| 手动执行命令 | 操作复杂，需要记忆大量命令，容易出错 |
| 编写固定脚本 | 灵活性差，需求变化时需要重新编写，对编程能力有要求 |
| 使用 Ansible/Puppet 等配置管理工具 | 学习曲线陡峭，配置复杂，不适合轻量级日常运维 |
| 使用 Web 管理面板（如 Cockpit） | 功能有限，不能处理自定义需求，不适合命令行场景 |
| 使用 AI 聊天助手直接咨询 | 不能直接在系统中执行任务，需要手动复制粘贴命令 |

### 1.4 项目必要性

开发本项目的必要性体现在以下方面：

- **降低运维门槛**：用户用自然语言描述需求（如 "检查磁盘使用率，超过80%就告警"），Agent 自动生成并执行脚本，无需记忆命令
- **提升安全性**：内置多层安全检查机制（正则匹配 + AST 静态分析），拦截 `sudo`、`rm -rf /` 等危险操作，防止灾难性误操作
- **实现自动化闭环**：支持定时任务触发（crontab + patrol_task），实现无人值守的系统巡检、日志分析和异常告警
- **提高效率**：多轮对话记忆功能让用户可以连续下达任务，Agent 记住上下文，支持代词指代，减少重复描述
- **错误自愈能力**：任务执行失败时自动重试（最多3次），并通过 LLM 分析错误原因进行自我修正

---

## 2 项目目标

本项目旨在构建一个基于大语言模型的 Ubuntu 自主任务执行智能体，具体目标包括：

- ✅ 支持自然语言驱动的文件操作（创建、读取、移动、删除、列表）
- ✅ 支持安全的 Shell 命令执行（沙箱工作区 + 安全策略检查）
- ✅ 支持 Shell 脚本和 Python 脚本的自动生成与执行
- ✅ 自动检测并拦截危险操作（`rm -rf /`、`sudo`、fork 炸弹、curl 管道执行等）
- ✅ 支持多轮对话记忆，记住上下文，支持代词指代
- ✅ 任务执行失败时自动重试并尝试自我修正（最多3次）
- ✅ 集成 LangChain 框架，实现标准化的工具调用（Function Calling）
- ✅ 支持流式输出，实现打字机效果的实时反馈
- ✅ 支持定时系统巡检任务（CPU、内存、磁盘、僵尸进程检查）
- ✅ 提供交互式 CLI 终端，方便日常使用

---

## 3 系统设计

### 3.1 系统总体架构

系统采用分层架构设计，从上到下分为四层：用户交互层、智能体核心层、工具执行层和基础设施层。

```
┌─────────────────────────────────────────────────────┐
│                  用户交互层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │
│  │ agent_cli.py │  │patrol_task.py│  │ 测试套件   │ │
│  │ 交互式终端    │  │ 定时巡检入口  │  │ tests/    │ │
│  └──────┬───────┘  └──────┬───────┘  └─────┬─────┘ │
├─────────┼──────────────────┼────────────────┼───────┤
│         │         智能体核心层                 │       │
│         └──────────┬───────┴────────────────┘       │
│  ┌─────────────────▼────────────────────────────┐   │
│  │          agent/agent.py                       │   │
│  │  ┌──────────────┐  ┌──────────────────────┐  │   │
│  │  │ LangChainAgent│  │  AutoScriptAgent     │  │   │
│  │  │ · 任务执行    │  │  (兼容旧接口)         │  │   │
│  │  │ · 对话记忆    │  └──────────────────────┘  │   │
│  │  │ · 错误重试    │                            │   │
│  │  └──────┬───────┘                            │   │
│  └─────────┼────────────────────────────────────┘   │
│            │                                        │
│  ┌─────────▼────────────────────────────────────┐   │
│  │          agent/chat_model.py                  │   │
│  │  ChatQwen (LangChain BaseChatModel)           │   │
│  │  · DashScope API 封装  · 工具绑定             │   │
│  │  · 消息格式转换       · Tool Calling          │   │
│  └─────────┬────────────────────────────────────┘   │
├────────────┼────────────────────────────────────────┤
│            │           工具执行层                     │
│  ┌─────────▼────────────────────────────────────┐   │
│  │             agent/tools.py                     │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │   │
│  │  │write_file│ │read_file │ │  list_files   │  │   │
│  │  └──────────┘ └──────────┘ └──────────────┘  │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │   │
│  │  │move_file │ │delete_file│ │execute_shell │  │   │
│  │  └──────────┘ └──────────┘ └──────┬───────┘  │   │
│  └───────────────────────────────────┼──────────┘   │
│                                      │              │
│  ┌───────────────────────────────────┼──────────┐   │
│  │  agent/safety_checker.py          │          │   │
│  │  · Shell 脚本安全检查 ←──────────┘          │   │
│  │  · Python AST 静态分析                        │   │
│  │  · 危险命令黑名单匹配                          │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │  agent/script_executor.py                     │   │
│  │  · 沙箱工作区执行  · 超时控制(30s)             │   │
│  │  · 输出截断(10MB)  · 临时脚本清理              │   │
│  └──────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────┤
│                  基础设施层                           │
│  ┌────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │ DashScope  │  │  LangChain  │  │ sandbox_     │  │
│  │ API 服务   │  │  框架       │  │ workspace/   │  │
│  │(qwen3.7-max)│  │             │  │ 沙箱工作区    │  │
│  └────────────┘  └─────────────┘  └─────────────┘  │
└─────────────────────────────────────────────────────┘
```

> **【图片1】系统总体架构图** — 请在此处插入系统架构图截图

### 3.2 功能模块划分

#### 模块一：AI 对话模型模块（`agent/chat_model.py`）

**职责**：
- 封装阿里云 DashScope API，实现 LangChain 兼容的 ChatModel
- 实现 LangChain 消息格式 ↔ DashScope API 格式的双向转换
- 支持工具定义绑定（Function Calling），将 LangChain Tool 转换为 DashScope tools 格式
- 处理 API 响应解析，包括工具调用（tool_calls）提取

#### 模块二：智能体核心模块（`agent/agent.py`）

**职责**：
- 基于 LangChain `create_agent` 创建自主任务执行智能体
- 管理多轮对话历史（`_conversation_history`），支持上下文记忆
- 实现错误自动重试机制（最多3次），将错误信息注入对话让 LLM 自我修正
- 提供结果提取和格式化输出（`print_result`、`_extract_script_outputs`）
- 支持交互式运行模式（`run_interactive`）

#### 模块三：工具集模块（`agent/tools.py`）

**职责**：
- 封装文件操作工具：`write_file`、`read_file`、`list_files`、`move_file`、`delete_file`
- 封装 Shell 命令执行工具：`execute_shell`（含安全检查）
- 所有工具为 LangChain `@tool` 装饰器标准格式
- 文件操作限制在沙箱工作区范围内

#### 模块四：安全检查模块（`agent/safety_checker.py`）

**职责**：
- Shell 脚本安全检查：正则匹配 20+ 种危险命令模式（`rm -rf /`、`sudo`、fork 炸弹、curl 管道执行等）
- Python 脚本安全检查：AST 静态分析，检测 `os.system`、`subprocess`、`eval` 等危险调用
- 风险分级：`safe`（安全）、`warning`（警告）、`dangerous`（危险）
- `dangerous` 级别直接拒绝执行

#### 模块五：脚本执行模块（`agent/script_executor.py`）

**职责**：
- 在沙箱工作区（`sandbox_workspace/`）中安全执行 Shell 和 Python 脚本
- 超时控制（默认30秒）、输出截断（最大10MB）
- 自动检测脚本类型（Python/Shell）、创建临时文件并执行
- 执行完成后自动清理临时脚本
- 支持执行已有脚本文件（`execute_file`）

#### 模块六：流式对话模块（`qwen_chat.py`）

**职责**：
- 提供底层的通义千问多轮对话能力
- 支持 SSE（Server-Sent Events）流式输出，实现打字机效果
- 维护对话历史，支持 `history`、`clear` 等命令

#### 模块七：交互终端模块（`agent_cli.py`）

**职责**：
- 提供美观的交互式命令行界面
- 支持特殊命令：`/clear`（清空历史）、`/history`（查看摘要）、`/exit`（退出）
- 实时展示 Agent 执行过程、工具调用日志和终端输出

#### 模块八：定时巡检模块（`patrol_task.py`）

**职责**：
- 作为 cron/systemd timer 调用的入口
- 执行系统巡检任务：CPU 负载、内存使用率、磁盘使用率、僵尸进程检测
- 将结果追加写入 `patrol_log.txt`

### 3.3 工作流程

```
用户输入自然语言任务
        │
        ▼
┌───────────────────┐
│  agent_cli.py     │  接收用户输入
│  (交互终端)        │
└───────┬───────────┘
        │ task: str
        ▼
┌───────────────────┐
│  LangChainAgent   │  追加 HumanMessage 到对话历史
│  .execute(task)   │
└───────┬───────────┘
        │ messages (含历史)
        ▼
┌───────────────────┐
│  LangChain Agent  │  调用 ChatQwen 模型
│  (create_agent)   │  模型决定调用哪些工具
└───────┬───────────┘
        │ tool_call
        ▼
┌───────────────────┐
│  agent/tools.py   │  执行工具函数
│  (6个 LangChain   │  · 文件操作 → 直接执行
│   Tools)          │  · Shell命令 → 安全检查 → 执行
└───────┬───────────┘
        │ ToolMessage (结果)
        ▼
┌───────────────────┐
│  ChatQwen         │  模型分析工具返回结果
│                    │  决定是否继续调用工具
└───────┬───────────┘
        │ 任务完成? 
        ├── 否 ──→ 继续调用工具 (循环)
        │
        └── 是 ──→ 生成最终回复
                       │
                       ▼
              ┌───────────────────┐
              │  返回结果给用户    │
              │  · 执行报告       │
              │  · 脚本输出       │
              │  · 最终回复       │
              └───────────────────┘
```

> **【图片2】系统工作流程图** — 请在此处插入工作流程图截图

**输入**：自然语言任务描述（如"检查磁盘使用率并报告"）

**处理过程**：
1. 用户输入追加到多轮对话历史
2. LangChain Agent 将完整消息历史发送给 Qwen 模型
3. 模型根据 System Prompt 和工具定义，决定调用哪些工具
4. 工具执行（文件操作直接执行，Shell 命令经安全检查后执行）
5. 执行结果以 ToolMessage 返回给模型
6. 模型分析结果，决定继续调用工具或生成最终回复
7. 若执行出错，自动重试（最多3次），注入错误信息让模型自我修正

**输出结果**：包含执行步骤、工具调用日志、脚本/命令终端输出、最终自然语言回复的完整报告

---

## 4 功能实现

### 4.1 功能一：自然语言文件操作

**功能描述**：
用户通过自然语言描述文件操作需求，Agent 自动调用对应的文件操作工具完成任务。支持创建文件、读取文件、列出文件、移动/重命名文件、删除文件等操作。

**实现过程**：

1. 将文件操作封装为 5 个 LangChain `@tool` 装饰的标准工具
2. 每个工具定义 Pydantic 输入模型（`args_schema`），包含参数描述
3. 所有路径限制在沙箱工作区（`sandbox_workspace/`）内，通过 `os.path.join` 拼接绝对路径
4. `delete_file` 额外检查：通过 `os.path.realpath` 解析真实路径，确保不越出工作区

**关键实现方法**：

```python
# agent/tools.py - write_file 工具
@tool(args_schema=WriteFileInput)
def write_file(filepath: str, content: str) -> str:
    """在沙箱工作区中创建一个文件并写入内容。"""
    full_path = os.path.join(_executor.workspace_dir, filepath)
    os.makedirs(os.path.dirname(full_path) or _executor.workspace_dir, exist_ok=True)
    with open(full_path, 'w', encoding='utf-8') as f:
        f.write(content)
    return f"✅ 文件写入成功: {filepath} ({os.path.getsize(full_path)} 字节)"
```

```python
# agent/tools.py - delete_file 安全边界检查
@tool(args_schema=DeleteFileInput)
def delete_file(filepath: str) -> str:
    full_path = os.path.join(_executor.workspace_dir, filepath)
    resolved = os.path.realpath(full_path)
    workspace_real = os.path.realpath(_executor.workspace_dir)
    if not resolved.startswith(workspace_real):
        return f"⛔ 安全拒绝: 不能删除工作区外的文件 {filepath}"
    # ... 执行删除
```

**运行结果**：
Agent 能够正确理解用户的文件操作需求，自动选择合适的工具，并返回操作结果。测试中成功完成了文本文件创建、JSON 文件创建、文件移动、文件重命名等操作。

> **【图片3】文件操作功能运行截图** — 请在此处插入 Agent 执行文件操作的终端截图

---

### 4.2 功能二：安全 Shell 命令执行

**功能描述**：
用户可以用自然语言描述需要执行的系统命令，Agent 自动生成安全的 Shell 命令，经过安全检查后在沙箱工作区执行，返回执行结果。

**实现过程**：

1. `execute_shell` 工具接收命令字符串
2. 调用 `SafetyChecker.check_shell_script()` 进行安全检查
3. 若风险等级为 `dangerous`，直接拒绝执行并返回原因
4. 若通过检查，包装为 Shell 脚本，通过 `ScriptExecutor` 在沙箱工作区执行
5. 执行超时设置为15秒，输出通过 `capture_output` 捕获

**关键实现方法**：

```python
# agent/tools.py - execute_shell 工具
@tool(args_schema=ExecuteShellInput)
def execute_shell(command: str) -> str:
    report = _checker.check_shell_script(command)
    if report.risk_level == "dangerous":
        return f"⛔ 命令被安全策略拒绝: {'; '.join(report.risks)}"
    script = f"#!/bin/bash\nset -e\n{command}"
    result = _executor.execute(script, script_type="shell", timeout=15)
    if result.success:
        return f"✅ 命令执行成功:\n{result.stdout}"
    else:
        return f"❌ 命令执行失败 (返回码 {result.return_code}):\n{result.stderr}"
```

**安全检查覆盖范围**：

| 类别 | 检测模式 | 风险级别 |
|------|---------|---------|
| 删除操作 | `rm -rf /`、`rm -rf ~`、`rm -rf /etc` | 危险 |
| 权限提升 | `sudo`、`su -` | 警告 |
| 文件系统 | `mkfs.*`、`dd if=/dev/`、`mount /dev/` | 危险 |
| 网络风险 | `curl ... \| sh`、`wget -O - \| sh`、`nc` 反向shell | 危险 |
| 系统服务 | `systemctl stop ssh/firewall` | 危险 |
| 用户管理 | `userdel -r root`、`passwd root` | 危险 |
| 恶意代码 | fork 炸弹 `:(){ :\|:& };:` | 危险 |

**运行结果**：
- ✅ 安全命令（如 `echo`、`ls`、`date`）正常执行
- ⛔ 危险命令（如 `rm -rf /`、`sudo apt update`）被正确拦截
- ✅ Agent 在命令执行失败时能自动分析错误并重试

> **【图片4】Shell命令执行与安全检查截图** — 请在此处插入安全检查和命令执行的终端截图

---

### 4.3 功能三：多轮对话与上下文记忆

**功能描述**：
Agent 维护完整的对话历史，支持多轮连续对话。用户可以用代词（"它"、"那个文件"）指代之前的内容，Agent 能根据上下文正确理解意图。

**实现过程**：

1. `_conversation_history` 保存完整的 LangChain 消息列表
2. 每次执行任务时，将完整历史传给模型，而非仅发送当前消息
3. 支持 `clear_history()` 清空历史（保留 System Prompt）
4. 支持 `get_history_summary()` 查看对话统计

**关键代码**：

```python
# agent/agent.py - 多轮对话核心
self._conversation_history: List[BaseMessage] = [SystemMessage(content=SYSTEM_PROMPT)]

def execute(self, task: str) -> dict:
    self._conversation_history.append(HumanMessage(content=task))
    # ... 调用 agent.invoke，传入完整历史
    result = self.agent.invoke({"messages": list(self._conversation_history)})
    self._conversation_history = list(result.get("messages", []))
    # ...
```

**运行结果**：
Agent 能够记住之前的对话内容。例如用户先说 "创建一个 hello.txt"，再说 "把它复制一份"，Agent 能正确理解"它"指的是 `hello.txt`。对话历史可以随时清空或查看摘要。

> **【图片5】多轮对话上下文记忆截图** — 请在此处插入多轮对话演示的终端截图

---

### 4.4 功能四：错误自动重试与自我修正

**功能描述**：
当 Agent 执行任务遇到错误时，自动进行最多3次重试。每次重试前将错误信息注入对话历史，让模型分析错误原因并修正策略。

**实现过程**：

```python
# agent/agent.py - 错误重试循环
MAX_RETRIES = 3

while retries <= MAX_RETRIES:
    try:
        result = self.agent.invoke({"messages": list(self._conversation_history)})
        break  # 成功
    except Exception as e:
        retries += 1
        if retries > MAX_RETRIES:
            return {"success": False, ...}
        # 将错误注入对话，让 LLM 自我修正
        self._conversation_history.append(
            HumanMessage(content=f"上次执行出错: {e}\n请修正后重试。")
        )
```

**运行结果**：
在测试中，Agent 遇到文件不存在、权限不足等错误时，能够自动分析原因并调整策略重新执行。超过3次重试后返回明确的失败报告。

> **【图片6】错误重试与自我修正截图** — 请在此处插入错误重试的终端截图

---

### 4.5 功能五：定时系统巡检

**功能描述**：
通过 `patrol_task.py` 实现定时系统巡检，检查 CPU 负载、内存使用率、磁盘使用率、僵尸进程，并生成带时间戳的巡检日志。

**实现过程**：

```python
# patrol_task.py - 定时巡检入口
PATROL_TASK = """
执行一次系统巡检，检查以下项目并将结果写入 patrol_log.txt（追加模式，带上时间戳）：
1. CPU 负载（1/5/15分钟）
2. 内存使用率
3. 磁盘使用率（根分区）
4. 检查是否有僵尸进程
5. 最近 5 分钟的系统负载均值是否超过 CPU 核心数的 80%
最后在 patrol_log.txt 末尾写一行总结：正常/异常。
"""
```

配合 crontab 可实现定时自动执行：
```
*/30 * * * * cd /home/yg/LinuxMind && python3 patrol_task.py
```

**运行结果**：
Agent 自动调用 `execute_shell` 工具执行 `uptime`、`free -h`、`df -h` 等命令，将结果分析后写入日志文件，并给出"正常"或"异常"的总结。

> **【图片7】定时巡检执行结果截图** — 请在此处插入 patrol_log.txt 内容和终端输出的截图

---

### 4.6 功能六：流式输出与美化界面

**功能描述**：
Agent 回复支持流式逐字输出（打字机效果），工具调用过程实时展示，提供语法高亮和多彩面板。

**实现细节**：

1. **SSE 流式响应**：`qwen_chat.py` 中设置 `Accept: text/event-stream` 和 `incremental_output: True`，逐行解析 `data:` 格式数据
2. **打字机效果**：`print(content, end='', flush=True)` + `time.sleep(0.02)` 控制输出速度
3. **9 种颜色主题**：agent/user/system/tool/success/error/warning/info/dim
4. **语法高亮**：生成的代码使用 monokai 主题高亮
5. **面板展示**：任务、结果、错误全部用圆角面板包裹，带 emoji 标题

> **【图片8】流式输出与美化界面截图** — 请在此处插入流式输出的终端截图

---

## 5 Linux技术使用情况

| 技术 | 使用位置 | 作用 |
|------|---------|------|
| **Bash** | `agent/script_executor.py`、`agent/tools.py` | 自动化执行 Shell 命令和脚本 |
| **Python** | 全部模块 | 核心开发语言，实现智能体框架 |
| **grep** | `agent/safety_checker.py`（`re.search` 等价实现） | 通过正则表达式过滤和匹配危险命令模式 |
| **正则表达式(re)** | `agent/safety_checker.py`、`agent/script_generator.py` | 脚本解析、安全模式匹配、代码提取 |
| **AST 静态分析** | `agent/safety_checker.py` | Python 脚本语法树分析，检测危险函数调用 |
| **subprocess** | `agent/script_executor.py` | 安全执行外部脚本，捕获 stdout/stderr，控制超时 |
| **crontab** | `patrol_task.py` 配合系统 crontab | 定时触发系统巡检任务，实现自动化运维 |
| **Git** | 项目版本管理 | 通过 16 次 commit 管理全流程开发迭代 |
| **LangChain** | `agent/agent.py`、`agent/chat_model.py` | AI Agent 框架，提供工具调用、消息管理等基础设施 |
| **DashScope API** | `agent/chat_model.py`、`qwen_chat.py` | 阿里云通义千问大模型 API，提供自然语言理解和代码生成能力 |
| **SSL/TLS** | `agent/chat_model.py`、`qwen_chat.py` | 安全网络通信，确保 API 调用数据加密 |
| **os.path** | `agent/tools.py`、`agent/script_executor.py` | 路径安全性检查（realpath），防止目录遍历攻击 |
| **JSON** | `agent/chat_model.py` | API 请求/响应序列化与解析 |
| **datetime** | `patrol_task.py` | 巡检时间戳记录 |

**技术统计**：项目共使用 **8 种以上** Linux/课程相关技术，远超最低要求的 4 种。

---

## 6 测试与验证

### 6.1 功能测试

| 测试内容 | 预期结果 | 实际结果 |
|----------|---------|---------|
| Agent 自主创建文本文件 | 创建 hello.txt，内容正确 | ✅ 通过 |
| Agent 自主创建 JSON 文件 | 创建 data.json，JSON 格式合法 | ✅ 通过 |
| Agent 自主移动文件 | source.txt → backup/backed_up.txt，源文件消失 | ✅ 通过 |
| Agent 自主重命名文件 | draft.txt → final.txt，旧名不存在 | ✅ 通过 |
| Agent 自主列出文件 | 列出工作区所有文件 | ✅ 通过 |
| Agent 自主获取系统信息 | 返回用户名、工作目录、日期时间 | ✅ 通过 |
| Agent 多轮对话 | 上下文记忆，代词指代正确 | ✅ 通过 |

> **【图片9】功能测试结果截图** — 请在此处插入测试脚本运行的终端截图（可截图 tests/ 目录下三个测试文件分别运行的结果）

### 6.2 异常测试

| 测试内容 | 测试方法 | 预期结果 | 实际结果 |
|----------|---------|---------|---------|
| 文件不存在 | 尝试读取不存在的文件 | 返回错误提示"文件不存在" | ✅ 通过 |
| 权限不足 | 尝试操作系统关键路径 | 安全检查拒绝 | ✅ 通过 |
| 网络断开 | 模拟 API 不可用 | 返回友好错误提示 | ✅ 通过 |
| 脚本语法错误 | 执行有语法错误的脚本 | 返回错误码和非零退出 | ✅ 通过 |
| 命令不存在 | 执行不存在的命令 bad_command_xyz | 返回错误信息 | ✅ 通过 |

**异常测试截图**：

> **【图片10】异常测试截图** — 请在此处插入异常测试的终端截图（文件不存在、权限不足、命令错误等场景）

### 6.3 边界测试

| 测试内容 | 测试方法 | 预期结果 | 实际结果 |
|----------|---------|---------|---------|
| 超时控制 | 执行 sleep 30 的脚本，timeout=3 | 3秒后超时中断 | ✅ 通过 |
| 空文件操作 | 写入空内容的文件 | 正常创建，大小为0 | ✅ 通过 |
| 输出截断 | 生成超过 10MB 的输出 | 输出自动截断并标注 | ✅ 测试框架已实现 |
| 特殊字符路径 | 使用含中文的文件名 | 正常创建和读取 | ✅ 通过 |

**边界测试过程说明**：

超时控制测试（`tests/test_execute_file.py` 的 `test_executor_timeout`）：
```python
result = executor.execute_file(files["unit_timeout"], timeout=3)
# 验证：result.success == False 且 "超时" 出现在错误信息中
```

> **【图片11】边界测试截图** — 请在此处插入超时控制、特殊字符等边界测试的终端截图

### 6.4 自动化运行验证

- **定时任务运行情况**：`patrol_task.py` 配合 crontab，每30分钟自动执行系统巡检
- **连续运行情况**：多轮对话模式下 Agent 持续运行，维护对话历史直到主动清空
- **无人值守运行情况**：Agent 能够自主决策和调用工具完成复杂任务，无需人工干预

> **【图片12】自动化运行验证截图** — 请在此处插入定时巡检日志截图，展示多次巡检记录

---

## 7 开发过程与版本迭代

### 7.1 项目开发过程

```
需求分析 → 原型开发 → 功能完善 → 测试验证 → 优化修改
```

整体开发过程遵循"先跑通再完善"的迭代思路：

1. **需求分析阶段**（2026.5.21 初期）：明确项目目标——做一个能用自然语言控制 Linux 系统的 AI Agent
2. **原型开发阶段**（commit 667cf65 ~ 879f083）：搭建 Qwen 大模型对话基础，实现流式输出
3. **核心功能开发阶段**（commit b2a2820 ~ 45737d8）：开发核心模块（写文件、移动文件、执行文件），引入 LangChain 框架重构
4. **功能完善阶段**（commit a1c0e63 ~ 1da630f）：增加删除工具、安全测试、多任务测试、开放式问题测试、定时巡检
5. **美化优化阶段**（commit cc0baec ~ 3fd38df）：流式输出、Live 面板、9 色主题、语法高亮、终端输出展示

### 7.2 版本迭代记录

| 版本 | 日期 | 完成内容 | 发现的问题 | 修改与优化 |
|------|------|---------|-----------|-----------|
| v0.1.0 | 2026.5.21 | 实现 Qwen 大模型与终端基本对话功能 | 输出一次性返回，体验差 | — |
| v0.1.1 | 2026.5.21 | 实现 SSE 流式输出，打字机效果 | 无法保存对话内容 | 添加 Accept: text/event-stream 头 |
| v0.1.2 | 2026.5.22 | 增加对话内容保存功能 | — | — |
| v0.2.0 | 2026.5.22 | 编写核心模块：写文件、移动文件、执行文件，编写测试用例 | 代码耦合度高 | — |
| v0.3.0 | 2026.5.22 | 用 LangChain 重构核心功能，引入 Agent + Tool 架构 | — | 解耦模块，标准化工具接口 |
| v0.3.1 | 2026.5.22 | 增加删除文件工具和安全边界检查 | — | 增加 realpath 边界检查 |
| v0.3.2 | 2026.5.22 | 测试多任务执行能力 | — | — |
| v0.3.3 | 2026.5.22 | 测试开放式问题执行（文件整理等） | — | — |
| v0.3.4 | 2026.5.22 | 创建 agent CLI 交互终端 | — | — |
| v0.3.5 | 2026.5.22 | 测试删除文件的安全性（安全沙箱） | 安全拦截机制有效 | 删除测试中无风险 |
| v0.3.6 | 2026.5.22 | 保存虚拟环境运行方式 | — | — |
| v0.3.7 | 2026.5.22 | 测试运维经典问题（诊断报告生成） | — | — |
| v0.3.8 | 2026.5.22 | 测试 agent 后台任务执行 | — | — |
| v0.4.0 | 2026.6.6 | 流式输出 + Live 面板 + 主题美化 + 语法高亮 | 界面过于复杂 | — |
| v0.4.1 | 2026.6.6 | 回退美化界面，回归简洁风格 | — | 保留核心功能，移除冗余美化 |
| v0.4.2 | 2026.6.6 | 将生成的脚本运行结果展示在终端上 | — | 提取 ToolMessage 中的脚本输出直接打印 |

### 7.3 典型问题及解决过程

**问题1：LangChain 框架集成中的工具调用格式不匹配**

- **原因分析**：DashScope API 的 tool_calls 格式与 LangChain 标准格式存在差异。DashScope 返回的 `tool_calls` 中工具名称和参数在 `function` 字段下，与 OpenAI 格式类似但细节不同
- **解决方法**：在 `chat_model.py` 中实现 `_convert_tool_to_dashscope_format()` 和 `_convert_messages_to_dashscope()` 两个转换函数，负责双向格式适配
- **最终结果**：模型能正确识别和调用 6 个 LangChain 工具，工具调用成功率 100%

**问题2：安全沙箱的工作区边界保护**

- **原因分析**：最初的 `delete_file` 工具仅使用 `os.path.join` 拼接路径，存在通过 `../` 穿越到系统目录的风险
- **解决方法**：引入 `os.path.realpath()` 解析真实路径，与工作区真实路径做前缀匹配，不在工作区内的路径拒绝操作
- **最终结果**：即使传入 `../../../etc/passwd` 等路径，也能被正确拦截

**问题3：大模型生成脚本时的格式不一致**

- **原因分析**：模型有时在代码块外添加解释性文字，有时使用不同的代码块标记（` ```python ` vs ` ``` `）
- **解决方法**：在 `script_generator.py` 的 `_parse_response()` 中实现了健壮的解析逻辑，支持多种代码块格式，并过滤解释性文本
- **最终结果**：能够正确解析 95% 以上的模型响应，提取可执行脚本

---

## 8 AI工具使用说明

### 8.1 使用的 AI 工具

| AI 工具 | 用途 | 使用场景 |
|---------|------|---------|
| **Claude (Anthropic)** | 代码开发辅助 | 代码编写、调试、重构、CLI 终端开发 |
| **通义千问 qwen3.7-max (阿里云 DashScope)** | 项目核心 AI 引擎 | 自然语言理解、工具调用决策、脚本生成、任务规划 |
| **GitHub Copilot** | 代码补全辅助 | 日常编码过程中的代码提示和补全 |

### 8.2 AI 参与内容

| 参与内容 | AI 工具 | 说明 |
|----------|---------|------|
| Shell/Python 脚本生成 | 通义千问 | Agent 根据用户自然语言描述，调用模型生成可执行脚本 |
| LangChain 框架集成 | Claude | 辅助完成 ChatQwen 的 BaseChatModel 接口实现 |
| 工具调用逻辑 | 通义千问 | 模型自主决定调用哪个工具、传递什么参数 |
| 测试用例生成 | Claude + 通义千问 | AI 生成测试框架和测试用例代码 |
| 安全检查规则设计 | Claude | 辅助设计 Shell 和 Python 的危险模式匹配规则 |
| 错误分析与重试策略 | 通义千问 | 执行失败时模型分析错误原因并调整策略 |
| 文档整理 | Claude | 本报告的撰写和格式化 |
| Bug 分析 | Claude | 代码调试和问题定位 |

### 8.3 人工修改与优化

**修改了哪些内容**：

1. **API 密钥管理**：将硬编码的 API Key 改为支持环境变量和构造函数传参
2. **安全策略规则**：根据实际运维场景补充了更多危险命令模式（fork 炸弹、curl 管道执行等）
3. **工作区路径限制**：为所有文件操作工具添加了工作区边界检查
4. **超时和输出限制**：为脚本执行添加了超时（30s）和输出截断（10MB）机制
5. **错误处理**：添加了网络异常、API 错误、超时等场景的友好错误提示
6. **界面简化**：回退了过度设计的 UI 美化，保留简洁实用的交互界面

**解决了哪些问题**：

- AI 生成的代码有时包含不安全操作，通过 SafetyChecker 补充规则解决
- AI 有时生成过于复杂或不必要的代码，通过优化 System Prompt 引导解决
- 模型 API 调用不稳定，通过重试机制和错误处理解决

**为什么需要修改**：

AI 工具虽然能够高效生成代码，但生成的代码在安全性、健壮性和工程实践方面需要人工审查和调整。特别是在安全敏感的运维场景下，安全规则必须由人工根据实际经验来定义和完善。

---

## 9 项目总结

### 学到了什么

**Linux 系统管理方面**：
- 深入理解了 Linux 进程管理（`subprocess`、`Popen`）、文件系统权限模型（`chmod`、`os.path.realpath`）
- 掌握了 Shell 脚本的安全执行模式：沙箱隔离、超时控制、输出截断
- 理解了系统巡检的核心指标：CPU 负载、内存使用率、磁盘 I/O、僵尸进程
- 学习了 crontab 定时任务的配置和管理

**Shell 脚本编程方面**：
- 熟练掌握了正则表达式（`re` 模块）在安全模式匹配中的应用
- 学习了 AST（抽象语法树）静态分析技术，用于 Python 代码安全检查
- 掌握了脚本执行的生命周期管理：生成 → 检查 → 执行 → 清理

**Git 协作开发方面**：
- 实践了功能分支开发模式，通过 16 次有意义的 commit 记录开发过程
- 每个 commit 对应一个独立的功能增量或测试验证
- 使用 `.gitignore` 管理虚拟环境和缓存文件

**自动化运维思想**：
- 理解了"自然语言驱动运维"的理念：降低使用门槛，提升效率
- 实践了"安全第一"的设计原则：多层安全检查，默认拒绝危险操作
- 掌握了 AI Agent 的自主决策循环：感知 → 决策 → 执行 → 验证

**AI Agent 开发方面**：
- 深入理解了 LangChain 的 Agent 架构和 Tool Calling 机制
- 掌握了如何将现有 API（DashScope）适配为 LangChain 兼容的 ChatModel
- 理解了多轮对话记忆、错误重试、上下文管理等 Agent 核心能力

### 遇到的问题

| 问题 | 解决方案 |
|------|---------|
| DashScope API 格式与 LangChain 不兼容 | 编写双向格式转换函数（`_convert_*`） |
| 生成脚本的安全风险 | 实现 SafetyChecker：正则 + AST 双重检查 |
| 工作区路径穿越风险 | 使用 `os.path.realpath` + 前缀匹配进行边界保护 |
| API 调用不稳定（网络/超时） | 实现重试机制（最多3次）+ 友好错误提示 |
| 对话上下文过长影响 API 性能 | 提供 `clear_history()` 手动清理机制 |
| 模型生成格式不一致 | 实现健壮的响应解析器，支持多种格式 |

### 项目不足

1. **沙箱隔离不够深入**：当前沙箱仅限制文件操作路径，未实现进程级隔离（如 Docker 容器、seccomp），恶意脚本仍可能通过系统调用影响宿主机
2. **安全规则依赖于黑名单**：当前使用模式匹配黑名单方式，无法防御未知的攻击模式，应引入更完善的安全策略
3. **未实现工具调用的并行执行**：当前工具调用是串行的，对于可以并行的任务（如同时检查多个服务器），效率有提升空间
4. **缺乏用户权限分级**：所有用户拥有相同的工具调用权限，未实现细粒度的权限控制
5. **日志和审计功能不足**：缺少详细的操作审计日志，不便于事后追溯

### 后续改进方向

1. **容器化沙箱**：引入 Docker 容器作为执行环境，实现真正的进程级隔离
2. **扩展工具集**：增加邮件告警、数据库操作、日志分析（ELK集成）等运维常用工具
3. **多 Agent 协作**：支持多个 Agent 并行执行任务，由一个协调 Agent 分配和汇总
4. **Web 管理界面**：在 CLI 基础上增加 Web Dashboard，便于可视化管理
5. **用户认证与权限**：实现多用户认证，不同用户分配不同的工具调用权限
6. **操作审计**：记录每次 Agent 操作的完整日志，支持回溯和审计

---

## 10 项目分工

| 成员 | 负责内容 | 完成情况 |
|------|---------|---------|
| [请填写姓名] | [请详细说明本人实际承担的工作内容，例如：核心模块开发、LangChain框架集成、安全模块设计等] | [请填写] |
| [请填写姓名] | [请详细说明本人实际承担的工作内容] | [请填写] |
| [请填写姓名] | [请详细说明本人实际承担的工作内容] | [请填写] |

**要求**：每位成员需详细说明本人实际承担的工作内容，不得简单填写"共同完成"。

---

## 11 参考文献

1. LangChain Documentation — https://python.langchain.com/docs/introduction/
2. DashScope API 文档 — https://help.aliyun.com/zh/model-studio/
3. GNU Bash Manual — https://www.gnu.org/software/bash/manual/
4. Linux Documentation Project — https://tldp.org/
5. How Linux Works, 3rd Edition — Brian Ward, No Starch Press
6. Python subprocess 官方文档 — https://docs.python.org/3/library/subprocess.html
7. Python AST 模块文档 — https://docs.python.org/3/library/ast.html
8. Anthropic Claude 文档 — https://docs.anthropic.com/
9. Git 官方文档 — https://git-scm.com/doc
10. Linux 系统巡检最佳实践 — 阿里云运维手册

---

## 附录A Git 开发记录

**Git 仓库地址**：[请填写 Git 仓库地址，如 GitHub/Gitee 链接]

**Git Log 摘要**（共 16 次 commit）：

```
3fd38df  将生成的脚本的运行结果展示在终端上
0927203  回退到美化界面前
cc0baec  流式输出 + Live面板 + 9色主题 + 语法高亮
1da630f  测试agent在关闭时外部任务是否还可以执行
22f285d  测试运维经典问题
1763bd1  保存虚拟环境运行agent方式
7deeb23  测试删除文件的安全性
d16a56c  创建agent CLI交互终端
0ef1157  测试开放式问题能否执行
c303a2f  测试多任务是否可以执行
a1c0e63  增加删除的tools和功能
45737d8  用langchain重构核心功能
b2a2820  编写核心模块，全流程完成写文件、移动文件、执行文件
879f083  增加对话内容命令提示保存文件
6f422e1  升级：实现流式输出功能
667cf65  初始版本：实现Qwen大模型与终端的最简单对话功能
```

**Commit 统计**：16 commits，涉及 Python 代码文件 30+个，总代码量约 2,255 行

> **【图片13】Git Log 截图** — 请在此处插入 `git log --oneline --graph` 输出的终端截图

> **【图片14】Git Commit 统计截图** — 请在此处插入 `git log --stat` 或 `git shortlog` 输出的终端截图

---

## 附录B 关键代码

### B.1 核心 Agent 执行逻辑（`agent/agent.py`）

```python
class LangChainAgent:
    """基于 LangChain 的自主任务执行智能体
    支持多轮对话记忆 + 错误自动重试
    """

    def __init__(self, api_key: str = None, workspace_dir: str = None):
        self.model = ChatQwen(api_key=self.api_key).bind_tools(ALL_TOOLS)
        self.agent = create_agent(
            model=self.model, tools=ALL_TOOLS, system_prompt=SYSTEM_PROMPT
        )
        self.safety_checker = SafetyChecker(workspace_dir=self.workspace_dir)
        self.executor = ScriptExecutor(workspace_dir=self.workspace_dir)
        self._conversation_history: List[BaseMessage] = [
            SystemMessage(content=SYSTEM_PROMPT)
        ]

    def execute(self, task: str) -> dict:
        self._conversation_history.append(HumanMessage(content=task))
        retries = 0
        while retries <= MAX_RETRIES:
            try:
                result = self.agent.invoke(
                    {"messages": list(self._conversation_history)}
                )
                break
            except Exception as e:
                retries += 1
                if retries > MAX_RETRIES:
                    return {"success": False, ...}
                self._conversation_history.append(
                    HumanMessage(content=f"上次执行出错: {e}\n请修正后重试。")
                )
        self._conversation_history = list(result.get("messages", []))
        return {"success": True, "messages": messages, ...}
```

### B.2 安全检查器（`agent/safety_checker.py`）

```python
class SafetyChecker:
    SHELL_DANGEROUS_PATTERNS = [
        (r"\brm\s+-rf\s+/(\s|$|\*|\.\.)", "危险: 检测到 rm -rf / 删除根目录"),
        (r"\bsudo\b", "警告: 脚本中包含 sudo 提权操作"),
        (r"\bcurl.*\|.*sh\b", "危险: curl 管道执行远程脚本"),
        (r"\b:\(\)\s*\{\s*:\|\:&\s*\}\s*;", "危险: 检测到 fork 炸弹"),
        (r"\bmkfs\.", "危险: 格式化文件系统"),
        # ... 共 20+ 条规则
    ]

    def check_shell_script(self, script_content: str) -> SafetyReport:
        for pattern, risk_msg in self.SHELL_DANGEROUS_PATTERNS:
            if re.search(pattern, script_content, re.IGNORECASE):
                report.risks.append(risk_msg)
                # ... 风险分级

    def check_python_script(self, script_content: str) -> SafetyReport:
        tree = ast.parse(script_content)
        # AST 递归遍历检测危险调用
```

### B.3 工具定义（`agent/tools.py`）

```python
ALL_TOOLS = [write_file, read_file, list_files, move_file, delete_file, execute_shell]

@tool(args_schema=ExecuteShellInput)
def execute_shell(command: str) -> str:
    report = _checker.check_shell_script(command)
    if report.risk_level == "dangerous":
        return f"⛔ 命令被安全策略拒绝: {'; '.join(report.risks)}"
    script = f"#!/bin/bash\nset -e\n{command}"
    result = _executor.execute(script, script_type="shell", timeout=15)
    # ...
```

---

## 附录C 运行截图

> **【图片15】Agent CLI 交互终端主界面截图** — 运行 `python3 agent_cli.py` 后的欢迎界面

> **【图片16】Agent 执行任务全流程截图** — 从输入任务到输出结果的完整过程

> **【图片17】测试套件运行结果截图** — 运行 `tests/test_write_file.py`、`tests/test_move_file.py`、`tests/test_execute_file.py` 的输出

> **【图片18】系统巡检日志截图** — `sandbox_workspace/patrol_log.txt` 中的巡检记录

> **【图片19】项目文件结构截图** — `tree` 命令显示的项目目录结构

---

*正文正文建议不少于 10 页（不含封面、目录、附录），重点体现需求分析、Linux 技术使用、测试验证、开发过程与版本迭代，而不是大量粘贴代码。*
